node_modules/
# Local secrets — never commit (Supabase service role, JWT, etc.)
.env
.env.local
.env.*
!.env.example
!.env.production.example
npm-debug.log*
*.log
coverage/
.DS_Store
tmp/

# Portadas de talleres subidas en local (Supabase es el destino en producción)
public/uploads/shop-covers/*
!public/uploads/shop-covers/.gitkeep
